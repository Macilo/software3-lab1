'use strict'

const a = 5
const b = 10

// using ordinary string
console.log('Fifteen is ' + (a + b) + ' and not ' + (2 * a + b) + '.')

// using template strings
console.log(`Fifteen is ${a + b} and not ${2 * a + b}.`)
